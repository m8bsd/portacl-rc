package templates

templ PostView(title string, body string) {
    <div class="card">
        <div class="card-body">
            <h2>{ title }</h2>
            <p>{ body }</p>
        </div>
    </div>
}
